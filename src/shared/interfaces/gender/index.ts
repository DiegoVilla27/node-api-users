/**
 * Represents the gender of a user.
 * 
 * Allowed values:
 * - 'male': Identifies as male.
 * - 'female': Identifies as female.
 * - 'other': Identifies as non-binary or another gender identity.
 */
export type Gender = 'male' | 'female' | 'other';