/**
 * Represents the role of a user within the system.
 * 
 * Allowed values:
 * - 'admin': Has full access and administrative privileges.
 * - 'guest': Has limited access, typically read-only or restricted features.
 */
export type Role = 'admin' | 'guest';